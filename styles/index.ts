import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    buttonDisabled: {
        backgroundColor: "#ccc",
        opacity: 0.5,
    },
});

export default globalStyles;