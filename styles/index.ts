import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    buttonDisabled: {
        backgroundColor: "#ccc",
        opacity: 0.5,
    },
    pageHeader:{
    color: "#fff",
    fontSize: 20,
    fontWeight: "600",
  },
});

export default globalStyles;